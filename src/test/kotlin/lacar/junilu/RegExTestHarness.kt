package lacar.junilu

class RegExTestHarness {
    fun showMatches(pattern: String, text: String) {
//        val regex = pattern.toRegex()
//
//        regex.findAll(text).forEach { matchResult ->
//            matchResult.groups.forEachIndexed { i, group ->
//                "Match group $i: ${group.value}".println()
//            }
//        }
    }
}